#define VERBOSE_MODE 1    // 0 = INFO-Warning-Error   / 1 = 0 + Debug / 2 = 0 + Benchmark / 3 = 0 + Debug + Benchmark
#define VERBOSE_OUTPUT 0  // 0 = Serial / 1 = SD Card (yyyymmdd.log)
#define SQL_ARRAY_SIZE 288 //  (0 to 99 = 100)
